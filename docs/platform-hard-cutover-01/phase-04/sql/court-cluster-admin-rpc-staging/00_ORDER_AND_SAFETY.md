# Order and safety

1. Confirm project_ref = `qyewbxjsiiyufanzcjcq` before any apply.
2. Abort if URL or dashboard shows `expuvcohlcjzvrrauvud`.
3. Owner GO required. Agent must not apply without explicit GO.
4. Apply only the three numbered SQL files in this folder, in order 10 → 20 → 30.
5. Run `99_VERIFY.sql` read-only after apply.
6. No wipe / DROP / reseed / Restore in this package.
7. Do not mutate Club rows created by Operator Acceptance.
