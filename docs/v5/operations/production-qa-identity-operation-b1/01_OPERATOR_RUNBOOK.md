# Operation B1 — Operator Runbook (Future)

**Production GO remains NO until exact Owner authorization is issued in a later pass.**

## 0) Preconditions

1. Operation A closed (gender) — unrelated, but note Production GO is separate.
2. Fresh Operation B preflight still shows 8 SAFE / 3 REFERENCED.
3. External allowlist generated by SELECT-only capture (not from docs).
4. Scheduled physical backup evidence recorded.
5. Owner issues exact GO: `APPROVE_OPERATION_B1_EXACT_EIGHT_ONLY`.

## 1) Generate protected allowlist (SELECT-only)

Write outside Git:

`C:\Users\Le Phong\Documents\PICK_VN-Secure-Backups\Operation-B1\operation-b1-allowlist-<UTC>.json`

Include exactly 8 identities with minimum fields (see `02_PREFLIGHT.md`).  
Compute SHA-256 companion file. Restrict NTFS ACLs.

## 2) Dry-run (default)

```bash
set PRODUCTION_PROJECT_REF=expuvcohlcjzvrrauvud
set OPERATION_B1_BATCH_ID=<uuid>
set ALLOWLIST_PATH=<path>
set ALLOWLIST_SHA256=<sha>
set DRY_RUN=true
node scripts/operations/production-qa-identity-operation-b1/preflight.mjs
node scripts/operations/production-qa-identity-operation-b1/execute-reversible-quarantine.mjs
```

Expect `mutationCalls=0`.

## 3) Authorized execute (only after Owner GO)

```bash
set DRY_RUN=false
set OWNER_PRODUCTION_GO=APPROVE_OPERATION_B1_EXACT_EIGHT_ONLY
set EXPLICIT_EXECUTE_CONFIRMATION=I_UNDERSTAND_THIS_MUTATES_PRODUCTION_QA_ONLY
node scripts/operations/production-qa-identity-operation-b1/execute-reversible-quarantine.mjs
node scripts/operations/production-qa-identity-operation-b1/postcheck.mjs
```

## 4) Rollback (only if required)

Use the protected original-state snapshot for the same `OPERATION_B1_BATCH_ID`.  
See `05_ROLLBACK_UNQUARANTINE.md`.

## Prohibited

- Do not process QA-01/QA-02/QA-03.
- Do not hard-delete Auth users.
- Do not reuse Operation A Owner GO.
- Do not print full emails/IDs in normal logs.
