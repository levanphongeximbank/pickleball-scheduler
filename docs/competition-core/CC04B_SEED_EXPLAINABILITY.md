# CC-04B — Seed Explainability

`SeedExplanation` example path:

```
Player A
  → Seed #2
  → Reason
  → Competition Elo + Performance + Manual adjustment
  → Final score
```

Fields: `code`, `title`, `message`, `participantId`, `seedNumber`, `path[]`, `reasons[]`, `finalScore`.

Generated by `runCanonicalSeedPipeline()` for each ranked participant.

No UI rendering in CC-04B.
