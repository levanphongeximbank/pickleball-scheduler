# Known Limitations — Phase 3D

1. No Production callers — Legacy TT engines remain SSOT.
2. Persistence ports are stubs only; `enablePersistence` defaults false.
3. Substitution amendments are always empty from mapper — workflow is Phase later (OD-05 representation only in contracts).
4. Lineup mapping is deferred to Phase 3E.
5. MLP composition / gender policy algorithms stay in format engines.
6. Root public export and official CI manifest await Integrator Wave 2.
7. Shadow comparators are not registered (Integrator + Owner later).
8. Format adapter `teamTournamentParticipantAdapters` is intentionally unchanged (parallel map-only surface).
