import { createContext } from "react";

/**
 * Communication runtime React context (COMMS-07).
 * Provider lives in CommunicationRuntimeProvider.jsx.
 */
export const CommunicationRuntimeContext = createContext(null);
