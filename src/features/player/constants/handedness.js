/** Phase 1C — handedness canonical values. */

export const HANDEDNESS = Object.freeze({
  RIGHT: "right",
  LEFT: "left",
  AMBIDEXTROUS: "ambidextrous",
  UNKNOWN: "unknown",
});

export const HANDEDNESS_VALUES = Object.freeze(Object.values(HANDEDNESS));
