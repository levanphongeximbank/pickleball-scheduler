/**
 * Phase 3G — deterministic ordering service surface.
 * Implementation lives in tieBreak.js (no Math.random).
 */

export {
  compareCandidatesForSeed,
  orderCandidatesDeterministically,
  deterministicOrdering,
} from "./tieBreak.js";
