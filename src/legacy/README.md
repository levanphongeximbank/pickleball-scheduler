# Legacy modules (draft reference — v3.5.1)

**Không import từ production.** Bản nháp song song — `src/pages/Tournament.jsx` vẫn là bản chính.

| Path | Ghi chú |
|------|---------|
| `Tournament.jsx` | Copy tham chiếu; không thay `pages/Tournament.jsx` |
| `engine-v1/` | Bản copy engine v1.2; production vẫn dùng `src/engine/` |
| `scheduler-v1/` | Copy scheduler; production vẫn dùng `src/scheduler/` |

Cleanup chỉ khi có approval rõ ràng.
