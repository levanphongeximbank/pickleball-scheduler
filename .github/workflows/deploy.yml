name: Deploy Production

on:
  push:
    branches:
      - main
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      # Phase 44B.0 Foundation Lock — mandatory pre-deploy gate.
      # Runs the canonical error-registry check + ownership boundary lock.
      # Any violation exits non-zero and blocks the deploy job.
      - name: Foundation Lock (error registry + ownership boundaries)
        run: npm run ci:foundation-lock

      - name: Lint
        run: npm run lint

      - name: Unit tests
        run: npm run test:unit

      - name: Build
        run: npm run build
        env:
          VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
          VITE_SUPABASE_ANON_KEY: ${{ secrets.VITE_SUPABASE_ANON_KEY }}
          VITE_SEED_DEMO: false
          VITE_RBAC_ENABLED: ${{ vars.VITE_RBAC_ENABLED || 'true' }}
          VITE_PAYMENT_MODE: ${{ vars.VITE_PAYMENT_MODE || 'dev' }}

      - name: Deploy to Vercel
        uses: amondnet/vercel-action@v25
        with:
          vercel-token: ${{ secrets.VERCEL_TOKEN }}
          vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
          vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }}
          working-directory: ./
          vercel-args: --prod
